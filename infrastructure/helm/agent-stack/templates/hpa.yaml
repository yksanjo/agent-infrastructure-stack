{{- if .Values.protocolAdapter.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "agent-stack.fullname" . }}-protocol-adapter
  labels:
    {{- include "agent-stack.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "agent-stack.fullname" . }}-protocol-adapter
  minReplicas: {{ .Values.protocolAdapter.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.protocolAdapter.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.protocolAdapter.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.protocolAdapter.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.protocolAdapter.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.protocolAdapter.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
